--- dwm.c	Fri Oct 28 18:43:14 2011 -0700
+++ dwm.c	Fri Oct 28 23:45:12 2011 +0100
@@ -1418,8 +1416,6 @@
 	}
 	XSync(dpy, False);
 	while(XCheckMaskEvent(dpy, EnterWindowMask, &ev));
-	if (m == selmon && (m->tagset[m->seltags] & m->sel->tags))
-		warp(m->sel);
 }
 
 void
